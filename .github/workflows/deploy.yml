name: deploy
on:
  push:
    branches: main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: pull
        working-directory: /www/website/
        run: git reset --hard && git pull
      - name: build
        working-directory: /www/website/
        run: npm i && npm run build